apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: llm-sidecar
spec:
  selector:
    app: inference-api
  ports:
  - port: 8000
    targetPort: 8000
    name: api
  type: ClusterIP
